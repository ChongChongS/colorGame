void myMatrix(int theX, int theY) {
 println("got it: "+theX+", "+theY);
 d[theX][theY].update();
}

void circleRadius(float value) {
  //if(mouseReleased)
  println("circleRadius" + value);
 //for (int x = 0; x<nx; x++) {
 //  for (int y = 0; y<ny; y++) {
 //    d[x][y].maxr = value;
 //  }
 //}
}

//button generate
public void Generate() {
  println("Press Generate Button");
  //根据参数，生成圆
  //for(int i = 0; i < totalPoints; i++)
  //{
  //  cs.add(new Circle(bodyRadius,randomRadius,circleRadius,speed));
  //}
}